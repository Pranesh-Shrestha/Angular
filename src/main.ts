import { enableProdMode } from '@angular/core';

console.log('✅ Placeholder Angular main.ts');

// Optional: use JwtModule just to make the import work
import { JwtModule } from '@auth0/angular-jwt';

console.log(JwtModule);
